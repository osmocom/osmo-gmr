# auto-generated by grc.converter

id: rach_demod
label: RACH Demodulation
category: '[GMR-1]'

parameters:
-   id: sps
    label: Samples-per-Symbol
    dtype: int
-   id: etoa
    label: Estimated TOA
    dtype: int
-   id: len_tag_key
    label: Length Tag Name
    dtype: string
    default: packet_len

inputs:
-   label: burst
    domain: stream
    dtype: complex

outputs:
-   domain: message
    id: pdus

templates:
    imports: import gnuradio.gmr1 as gmr1
    make: gmr1.rach_demod(${sps}, ${etoa}, ${len_tag_key})

file_format: 1
