# auto-generated by grc.converter

id: gsmtap_sink
label: GSMTap Sink
category: '[GMR-1]'

parameters:
-   id: host
    label: Host
    dtype: string
    default: 127.0.0.1
-   id: port
    label: Port
    dtype: int
    default: '4729'

inputs:
-   domain: message
    id: pdus

templates:
    imports: import gnuradio.gmr1 as gmr1
    make: gmr1.gsmtap_sink(${host}, ${port})

file_format: 1
