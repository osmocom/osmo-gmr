# auto-generated by grc.converter

id: burst_to_tagged_stream
label: Burst to Tagged Stream
category: '[GMR-1]'

parameters:
-   id: max_length
    label: Maximum burst length
    dtype: int
-   id: len_tag_key
    label: Length Tag Name
    dtype: string
    default: packet_len

inputs:
-   domain: stream
    dtype: complex

outputs:
-   domain: stream
    dtype: complex

templates:
    imports: import gnuradio.gmr1 as gmr1
    make: gmr1.burst_to_tagged_stream(${max_length}, ${len_tag_key})

file_format: 1
