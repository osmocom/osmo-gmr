# auto-generated by grc.converter

id: rach_detect_fft
label: RACH FFT Detection
category: '[GMR-1]'

parameters:
-   id: samp_rate
    label: Sample Rate
    dtype: real
    default: samp_rate
-   id: overlap_ratio
    label: Overlap ratio
    dtype: int
    default: '2'
-   id: threshold
    label: Detection Threshold
    dtype: float
    default: '8.5'
-   id: burst_length
    label: Burst length
    dtype: int
    default: int(3 * samp_rate * 15e-3)
-   id: burst_offset
    label: Burst offset
    dtype: int
    default: '0'
-   id: peak_freq
    label: Peak Frequency
    dtype: float
    default: (math.pi / 4.0) * (23400.0 / samp_rate)
-   id: len_tag_key
    label: Length Tag Name
    dtype: string
    default: packet_len

inputs:
-   domain: stream
    dtype: complex

outputs:
-   label: burst
    domain: stream
    dtype: complex

templates:
    imports: |-
        import math
        import gnuradio.gmr1 as gmr1
    make: gmr1.rach_detect_fft(${samp_rate}, ${overlap_ratio}, ${threshold}, ${burst_length},
        ${burst_offset}, ${peak_freq}, ${len_tag_key})

file_format: 1
